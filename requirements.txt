# Персональний помічник — залежності
# Запуск: pip install -r requirements.txt

# Зовнішні бібліотеки
colorama>=0.4.6      # Кольоровий текст у терміналі (кросплатформно: Windows/Mac/Linux)
rich>=13.0.0         # Красивий вивід: таблиці, панелі, кольори, прогрес-бар

# Стандартні модулі Python 3.10+ (встановлювати не потрібно):
#   csv       — читання/запис CSV файлів (storage.py)
#   os        — робота з файловою системою (storage.py)
#   re        — регулярні вирази для валідації (contacts.py)
#   datetime  — робота з датами, дні народження (contacts.py)
#   typing    — анотації типів (всі файли)
